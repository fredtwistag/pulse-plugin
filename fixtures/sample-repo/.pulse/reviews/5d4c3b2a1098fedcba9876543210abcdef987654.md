---
id: review-5d4c3b2
sha: 5d4c3b2a1098fedcba9876543210abcdef987654
base: main
engineer: fred
created: 2026-05-12T11:14:55Z
verdicts:
  spec-conformance: pass
  security-regression: pass
  convention-drift: warning
  anti-pattern-repetition: warning
  performance-pitfalls: fail
  test-integrity: warning
  dependency-hygiene: pass
  data-api-safety: pass
overall: fail
---

# Guard review — 5d4c3b2

The audit-viewer-ui PR — the feature that lists audit events per-org. One performance fail (N+1 in the viewer's enrichment loop), plus a cluster of warnings.

## Summary

5/8 pass, 3 warnings, 1 fail. Engineer either fixes or files an override.

## Findings

### spec-conformance
*pass* — diff implements the listed acceptance items and nothing more. Pagination, CSV export, per-org filter all present.

### security-regression
*pass* — no banned patterns, no secrets.

### convention-drift
*warning* — three new service files under `src/services/audit/` use camelCase filenames against the project rule "Service files under src/services use kebab-case filenames."

- `src/services/audit/eventEnricher.ts` — rename to `event-enricher.ts`.
- `src/services/audit/csvExporter.ts` — rename to `csv-exporter.ts`.
- `src/services/audit/orgFilter.ts` — rename to `org-filter.ts`.

### anti-pattern-repetition
*warning* — the same `try { ... } catch (e) { logger.warn(e); return null; }` swallow-and-return-null idiom appears in 4 service files. Lift to a `safeCall` helper or surface failures to the caller; null returns mask real problems downstream.

- `src/services/audit/event-enricher.ts:18` (also in `csv-exporter.ts:24`, `org-filter.ts:31`, `viewer-loader.ts:42`)

### performance-pitfalls
*fail* — **N+1 query** in the viewer's event enricher.

- `src/services/audit/eventEnricher.ts:42-56` — `for (const event of events) { const actor = await db.users.findById(event.actorId); }`. Batch with `db.users.findMany({ id: { in: events.map(e => e.actorId) } })` then build a lookup map.

This will be visible in p95 for any org with >50 audit events. Block on it.

### test-integrity
*warning* — acceptance criterion "CSV export available to admins" has no test in this diff. Two of the new tests are snapshot-only on the rendered list component; recommend at least one assertion on observable behavior (sort order, filter application).

- `src/services/audit/__tests__/csv-exporter.test.ts` — missing.
- `src/components/audit/__tests__/AuditList.test.tsx:14` — snapshot-only.
- `src/components/audit/__tests__/AuditList.test.tsx:38` — snapshot-only.

### dependency-hygiene
*pass* — no dependency manifest changes.

### data-api-safety
*pass* — no schema changes, no public-API surface changes. The new admin GET /audit endpoint is additive; response shape is a new contract.
