---
description: Pulse Arch — propose 2-3 implementation paths and write an ADR for the chosen one.
argument-hint: "[<task slug or spec path>]"
---

Invoke the `pulse-arch` skill. Read the referenced spec under `docs/pulse/epics/.../tasks/$ARGUMENTS/spec.md` (or the path provided). Inspect the existing codebase and prior ADRs. Propose 2-3 implementation paths with trade-offs, recommend one, then write `docs/pulse/adr/ADR-NNN-<slug>.md` and update the parent task's `links.adr`.

Target: $ARGUMENTS
